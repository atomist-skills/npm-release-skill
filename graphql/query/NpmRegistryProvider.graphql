query NpmRegistryProvider($id: ID) {
  NpmJSRegistryProvider(id: $id) {
    id
    name
    scope
    url
    credential {
      ... on Password {
        secret
      }
      owner {
        login
      }
      id
    }
  }
}
